# 0.6: New note in Single page app diagram

```mermaid
sequencediagram
    participant browser
    participant server

    note left of browser: the user creates a <br/> new note

    note over browser: the handler function that <br/> incorporates the new note <br/> is already activated

    browser->>+server: post https://studies.cs.helsinki.fi/exampleapp/new_note_spa
    note over browser, server: the payload of the post request is a json file including "content" and "date"
    server-->>-browser: status 201:  note created
```
